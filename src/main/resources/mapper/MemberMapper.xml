<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gym.mapper.MemberMapper">

    <select id="selectById" resultType="com.gym.pojo.Member">
        SELECT *
        FROM member
        WHERE member_id = #{memberId}
    </select>

    <select id="selectByPhone" resultType="com.gym.pojo.Member">
        SELECT *
        FROM member
        WHERE member_phone = #{memberPhone}
    </select>

    <select id="selectAll" resultType="com.gym.pojo.Member">
        SELECT *
        FROM member
    </select>

    <select id="selectByKeyword" resultType="com.gym.pojo.Member">
        SELECT *
        FROM member
        WHERE member_name LIKE CONCAT('%', #{keyword}, '%')
           OR member_phone LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <insert id="insert">
        INSERT INTO member (member_password, member_name, member_gender, member_age, member_height, memberweight, member_phone, card_time, card_class, card_next_class)
        VALUES (#{memberPassword}, #{memberName}, #{memberGender}, #{memberAge}, #{memberHeight}, #{memberweight}, #{memberPhone}, #{cardTime}, #{cardClass}, #{cardNextClass})
    </insert>

    <update id="update">
        UPDATE member
        SET member_password = #{memberPassword},
            member_name = #{memberName},
            member_gender = #{memberGender},
            member_age = #{memberAge},
            member_height = #{memberHeight},
            memberweight = #{memberweight},
            member_phone = #{memberPhone},
            card_time = #{cardTime},
            card_class = #{cardClass},
            card_next_class = #{cardNextClass}
        WHERE member_id = #{memberId}
    </update>

    <delete id="deleteById">
        DELETE FROM member
        WHERE member_id = #{memberId}
    </delete>

    <select id="selectTotalCount" resultType="int">
        SELECT COUNT(*)
        FROM member
    </select>

</mapper>